enum InterviewType { technical, behavioral, hr, mixed }

enum InterviewStatus { inProgress, completed, cancelled }

enum ExperienceLevel { intern, junior, mid, senior }

enum MessageSender { user, ai }
